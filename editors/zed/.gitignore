# Rust/wasm build output for the Zed extension (regenerated by `make build-zed`).
/target/
# tree-sitter-ingle is its own git repo; its node_modules never belong to the main tree.
tree-sitter-ingle/node_modules/
