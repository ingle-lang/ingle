# Generated by `make kernel` (docs/design/kernel-freestanding.md) — the source is checked in
# (hello.em, platform.c, boot.S, kernel.ld); these build artifacts are not. runtime.o is the real
# src/runtime.c cross-compiled -DEMBER_FREESTANDING.
hello.c
*.o
*.elf
emit.err
