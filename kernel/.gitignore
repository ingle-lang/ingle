# Generated by `make kernel` (docs/design/kernel-freestanding.md) — the source is checked in
# (*.em, platform.c, timer.c, boot.S, vectors.S, kernel.ld); these build artifacts are not. The *.c
# below are emitted from the *.em by `emberc --emit=c`; runtime.o is src/runtime.c cross-compiled.
hello.c
faultdemo.c
timerdemo.c
*.o
*.elf
emit.err
